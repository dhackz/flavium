- project:
    check:
      jobs:
        - build-dashboard
        - build-backend
        - unittest-backend
    gate:
      jobs:
        - build-dashboard
        - build-backend
        - unittest-backend
